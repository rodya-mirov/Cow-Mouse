using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace CowMouse.Utilities
{
    public enum VerticalDirection
    {
        UP = -1,
        NEUTRAL = 0,
        DOWN = 1
    }

    public enum HorizontalDirection
    {
        LEFT = -1,
        NEUTRAL = 0,
        RIGHT = 1
    }
}
